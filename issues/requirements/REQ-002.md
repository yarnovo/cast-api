---
id: REQ-002
title: 加 hermes / akong_skills / akong_tools / agent_skills / agent_akong_tools 5 表 + endpoints (老板 5-9 拍 · agent 运行环境资源抽象)
status: in-progress
assignee: cast-api-maintainer
created: 2026-05-09
parent_scenario: akong-hermes-architecture-upgrade
related_repos:
  - akong-hermes (上游 · 抽象 + Loader/Resolver 库)
  - cast-agents (下游 · /api/agent/run 走 DynamicHermesLoader)
  - meta-hermes (下游 · lifespan 同步 yaml 到 RDS)
---

## 描述

老板 5-9 拍: agent runtime 资源 (soul/playbook/style + skills + tools) 抽象成 Hermes。
- 静态 hermes (yaml in repo) + 动态 hermes (RDS row) 双源
- 静态 skill/tool (代码包) + 动态 skill/tool (LLM 生成 code · sandbox 跑) 双源
- meta agent 通过 create_agent 对话动态创建

cast-api 加 5 表 + endpoints 提供 RDS 后端。

## acceptance

1. 5 表落库 · 字段完整 (hermes / akong_skills / akong_tools / agent_skills / agent_akong_tools)
2. alembic migration 双向跑过 (`upgrade head` + 回退 + 再 upgrade)
3. Endpoints 工作:
   - `POST/GET/PUT/DELETE /api/hermes`
   - `POST/GET/DELETE /api/skills`
   - `POST/GET/DELETE /api/akong-tools`
   - `POST/DELETE /api/agents/{id}/skills`
   - `POST/DELETE /api/agents/{id}/tools-link`
4. Hermes 创建时 skills/tools 引用支持 static (auto-upsert minimal skill row) + dynamic (validate skill_id 已存在)
5. tool kind 校验 (builtin / webhook / http_api / dynamic_python · 别的 422)
6. pytest 覆盖 ≥ 12 case · 全绿
7. CONTRACTS.md 加下游 (cast-agents / meta-hermes / akong-hermes)
8. 不破坏现有 92 cases

## 不做

- 不接 sandbox 真跑 dynamic_python (老板 5-9 拍 · 另一仓做)
- 不动老 platform `tools` 表 (registry · 'cast.post' 等 · 跟新 `akong_tools` 表互不冲突)
- 不动 cast-app 前端
- 不在本仓写 DynamicHermesLoader 客户端 (那是 akong-hermes 的事)

## 时间线

- 2026-05-09 创建 + assignee 自接 + 起 feat/hermes-tables-and-endpoints 分支
